import { Button } from '@mui/material'

export const Controls = () => {
  return (
    <div className='controls'>
      <Button variant='contained'>Play/Pause</Button>
      <Button variant='contained'>x2</Button>
    </div>
  )
}
